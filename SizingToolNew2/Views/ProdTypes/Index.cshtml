@model IEnumerable<SizingToolNew2.Models.ProdType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProdTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProdTypeDesc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Memo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProdTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProdTypeDesc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Memo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProdTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProdTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProdTypeId })
        </td>
    </tr>
}

</table>
